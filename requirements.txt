# 运行时底座依赖清单
#
# 共享集群不在 PyPI 上，用 VCS 依赖装 —— 这也顺带证明了「底座真的依赖共享包」：
# 依赖关系出现在安装产物里，而不是只写在文档里。
ecom-agent-shared @ git+https://github.com/rchzc/ecom-agent-shared.git@main

# 编排：状态图那一条路径用它；自研 Loop 路径不依赖它
langgraph>=0.2
